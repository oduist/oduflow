{"config":{"lang":["en"],"separator":"[\\s\\-]+","pipeline":["stopWordFilter"],"fields":{"title":{"boost":1000.0},"text":{"boost":1.0},"tags":{"boost":1000000.0}}},"docs":[{"location":"","title":"Oduflow","text":"<p>AI-first Odoo development and CI tool with reusable database templates, powered by MCP.</p>"},{"location":"#getting-started","title":"Getting Started","text":"<p>Welcome to the Oduflow documentation. This site is under construction.</p>"},{"location":"docker/","title":"Running Oduflow in Docker","text":"<p>Oduflow can run as a Docker container. Since it manages other Docker containers (Odoo environments, PostgreSQL, etc.), it uses the Docker-out-of-Docker pattern \u2014 the host's Docker socket is mounted into the container.</p>"},{"location":"docker/#build","title":"Build","text":"<pre><code>docker build -t oduflow .\n</code></pre>"},{"location":"docker/#run","title":"Run","text":""},{"location":"docker/#minimal-example","title":"Minimal example","text":"<pre><code>docker run -d \\\n  --name oduflow \\\n  -p 8000:8000 \\\n  -v /var/run/docker.sock:/var/run/docker.sock \\\n  -v oduflow_data:/srv/oduflow_data_1 \\\n  oduflow\n</code></pre>"},{"location":"docker/#with-env-file","title":"With <code>.env</code> file","text":"<pre><code>docker run -d \\\n  --name oduflow \\\n  --env-file .env \\\n  -p 8000:8000 \\\n  -v /var/run/docker.sock:/var/run/docker.sock \\\n  -v oduflow_data:/srv/oduflow_data_1 \\\n  oduflow\n</code></pre>"},{"location":"docker/#full-example-with-all-typical-options","title":"Full example with all typical options","text":"<pre><code>docker run -d \\\n  --name oduflow \\\n  --restart unless-stopped \\\n  --env-file .env \\\n  -p 8000:8000 \\\n  -v /var/run/docker.sock:/var/run/docker.sock \\\n  -v oduflow_data:/srv/oduflow_data_1 \\\n  -v /etc/oduflow:/etc/oduflow \\\n  oduflow\n</code></pre>"},{"location":"docker/#volume-mounts","title":"Volume Mounts","text":"Mount Purpose <code>/var/run/docker.sock</code> Required. Gives Oduflow access to the host Docker daemon to manage Odoo containers, PostgreSQL, Traefik, etc. <code>/srv/oduflow_data_1</code> Oduflow data directory (<code>ODUFLOW_HOME</code>). Contains workspaces, templates, port registry. Use a named volume or a host path to persist data across container restarts. <code>/etc/oduflow</code> License key storage. Mount if you have a commercial license."},{"location":"docker/#networking","title":"Networking","text":"<p>The Oduflow container must be on the same Docker network as the containers it creates. The simplest approach is to connect it to <code>oduflow-net</code> after initialization:</p> <pre><code># 1. Start Oduflow\ndocker run -d --name oduflow -p 8000:8000 \\\n  -v /var/run/docker.sock:/var/run/docker.sock \\\n  -v oduflow_data:/srv/oduflow_data_1 \\\n  oduflow\n\n# 2. Initialize shared infrastructure (creates oduflow-net, PostgreSQL, etc.)\ndocker exec oduflow oduflow init\n\n# 3. Connect Oduflow to the shared network\ndocker network connect oduflow-net oduflow\n\n# 4. Initialize instance directories\ndocker exec oduflow oduflow init-instance\n</code></pre> <p>Alternatively, start with <code>--network oduflow-net</code> if the network already exists.</p>"},{"location":"docker/#initialization","title":"Initialization","text":"<p>On first run, you need to initialize the shared infrastructure and instance:</p> <pre><code># Create shared Docker network, PostgreSQL container\ndocker exec oduflow oduflow init\n\n# Connect to the shared network\ndocker network connect oduflow-net oduflow\n\n# Create instance directories (workspaces, templates)\ndocker exec oduflow oduflow init-instance\n</code></pre> <p>To set up a template database:</p> <pre><code># From scratch (clean Odoo with specified modules)\ndocker exec oduflow oduflow init-template --odoo-image odoo:17.0 --modules base,web,contacts\n\n# Or import from a running Odoo instance\ndocker exec oduflow oduflow call import_template_from_odoo https://my-odoo.example.com master_password\n</code></pre>"},{"location":"docker/#environment-variables","title":"Environment Variables","text":"<p>All environment variables from <code>.env.example</code> are supported. Key ones for Docker:</p> Variable Default Description <code>ODUFLOW_TRANSPORT</code> <code>http</code> Transport mode: <code>http</code> or <code>stdio</code> <code>ODUFLOW_HOST</code> <code>0.0.0.0</code> Bind address <code>ODUFLOW_PORT</code> <code>8000</code> HTTP port <code>ODUFLOW_AUTH_TOKEN</code> (empty) Bearer token for MCP auth (empty = disabled) <code>ODUFLOW_HOME</code> <code>/srv/oduflow_data_1</code> Data directory <code>EXTERNAL_HOST</code> <code>localhost</code> Hostname used in generated URLs"},{"location":"docker/#docker-compose","title":"Docker Compose","text":"<pre><code>services:\n  oduflow:\n    build: .\n    ports:\n      - \"8000:8000\"\n    volumes:\n      - /var/run/docker.sock:/var/run/docker.sock\n      - oduflow_data:/srv/oduflow_data_1\n      - oduflow_license:/etc/oduflow\n    env_file: .env\n    restart: unless-stopped\n\nvolumes:\n  oduflow_data:\n  oduflow_license:\n</code></pre> <p>After <code>docker compose up -d</code>, run initialization:</p> <pre><code>docker compose exec oduflow oduflow init\ndocker network connect oduflow-net oduflow-oduflow-1\ndocker compose exec oduflow oduflow init-instance\n</code></pre>"},{"location":"docker/#security-notes","title":"Security Notes","text":"<ul> <li>Mounting the Docker socket gives the container full control over the host Docker daemon. This is equivalent to root access on the host. Only run Oduflow in trusted environments.</li> <li>Set <code>ODUFLOW_AUTH_TOKEN</code> to protect the MCP endpoint.</li> <li>Set <code>ODUFLOW_UI_PASSWORD</code> to protect the Web UI.</li> </ul>"},{"location":"docker/#privileged-mode-and-fuse-overlayfs","title":"Privileged Mode and fuse-overlayfs","text":"<p>Oduflow uses <code>fuse-overlayfs</code> for efficient filestore sharing when templates exceed <code>ODUFLOW_OVERLAY_THRESHOLD_MB</code> (default: 50 MB). This requires the <code>/dev/fuse</code> device inside the container.</p> <p>If your templates are small (under the threshold), Oduflow falls back to simple file copy and no special privileges are needed.</p> <p>For large templates, run with the fuse device:</p> <pre><code>docker run -d \\\n  --name oduflow \\\n  --device /dev/fuse \\\n  --cap-add SYS_ADMIN \\\n  -p 8000:8000 \\\n  -v /var/run/docker.sock:/var/run/docker.sock \\\n  -v oduflow_data:/srv/oduflow_data_1 \\\n  oduflow\n</code></pre> <p>Alternatively, raise the threshold to avoid overlayfs entirely:</p> <pre><code>ODUFLOW_OVERLAY_THRESHOLD_MB=999999\n</code></pre>"}]}